package search;

public class p2 {
	
	//binary search
	
	public static int binarysearch(int[]arr, int key, int n) {
		
		int left = 0;
		int right =n;
		
		while (left<=right) {
			int mid = (left+right)/2;
			
			if(key<arr[mid]) {
				right = mid-1;
				}
			else if(key>arr[mid]) {
				left = mid+1;
			}else {
				return mid;
			}
			
		}
		return -1;
	}

	public static void main(String[] args) {
		
		int[] arr = {1,2,5,7,8,10,11,13};
		int n = arr.length;
		int key = 13;
		
		int ans = binarySearch(arr, key, n);
		System.out.println(ans);
		
		
		// binary search 검색 api로 제공
		
		arr.bi
		

	}

}
